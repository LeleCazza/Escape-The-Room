<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="escape.room">
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EscapeRoom">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CameraActivity"/>
        <activity android:name=".ComputerLoginActivity"/>
        <activity android:name=".ComputerDesktopActivity"/>
        <activity android:name=".ComputerMailActivity"/>
        <activity android:name=".CestinoActivity"/>
        <activity android:name=".CassettoActivity"/>
        <activity android:name=".BancoActivity"/>
        <activity android:name=".LavagnaActivity"/>
    </application>

</manifest>